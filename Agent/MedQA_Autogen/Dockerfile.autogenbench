# 1) autogenbench가 한 번 빌드해 둔 기본 이미지를 베이스로 사용
FROM autogenbench:default

# 2) 평소 벤치마크에서 공통으로 필요한 패키지 목록
#    (HumanEval/GAIA/MedQA 등 자신의 사용 패키지로 채우세요)
COPY bench-requirements.txt /opt/bench-requirements.txt

# 3) 미리 설치(이미지에 굽기)
RUN python -m pip install --upgrade pip && \
    pip install -r /opt/bench-requirements.txt && \
    rm -rf /root/.cache/pip

